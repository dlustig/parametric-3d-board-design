# Gate results

One row per gate from SPEC §13 and the Slice-1 proof (§15). Updated as gates run. Evidence is the named test file and the command's summary line; numbers are from the run recorded, on this development machine.

| Gate | Status | Evidence |
| --- | --- | --- |
| §15 Slice-1 interaction (a–j) | PASS | `e2e/interaction-proof.spec.ts`, `e2e/select-tool.spec.ts`; chromium 13, firefox 13, webkit 13, chromium-touch 5 passed; 93/93 over `--repeat-each 3` (chromium/firefox/chromium-touch). Library quirks: `docs/decisions/2026-09-24-slice1-interaction.md`. |
| G1 Crossing identity | PASS (unit) | `src/geometry/resolve.test.ts` (polylines crossing twice, endpoint move, width, vertex insert both halves, start-point delete, transform then unrelated edit, hairpin non-steal, round-trip). Store-level undo/redo covered in `src/editor/store.test.ts`. |
| G2 Occurrence scope | PASS (unit) | `src/geometry/resolve.test.ts` (3×3 field, override cell, rows shrink, detach-with-override, all-instances in overridden cell, nested intermediate context, JSON round-trip). |
| G3 Transform parity | PASS (unit) + §15 (b)(c)(d)(h) | `src/geometry/affine.test.ts`, `expand.test.ts`, `commands.test.ts` (mirror algebra), `e2e/interaction-proof.spec.ts`. |
| G4 Edge classes | PASS (unit) | `src/geometry/intersections.test.ts` (15° unequal, endpoint, collinear, 5°/175°, self-crossing, triple, 1 mm apart, near-joint, occluded region, lattice touching in definition and world space). |
| G5 Export correctness and parity | PASS | `e2e/crossing-proof.spec.ts` (crossing subset, Task 9, unchanged): chromium 7, firefox 7, webkit 7 passed. `e2e/export-parity.spec.ts` (Fixture F, Task 17): `pnpm test:e2e --project=chromium --project=firefox --project=webkit e2e/export-parity.spec.ts` → chromium 5, firefox 5, webkit 5 passed. Fixture F (399×399 mm, 425 eligible intersections, all real design geometry, none crafted for the test) exported standalone (`window.__cbpd.exportSvg()`) and compared against the editor's Board region (`window.__cbpd.getScene()` + `rasterizeEditorBoard`), both rasterised at a fixed 800×800 px (`deviceScaleFactor: 1` — webkit's Desktop Safari profile defaults to 2, which would otherwise screenshot the editor at 1600×1600 against the SVG raster's fixed 800×800). **Max per-pixel diff, excluding the 3×3/8-bin anti-aliased-edge mask (SPEC's ≤ 8/255 gate): chromium 1/255, firefox 1/255, webkit 1/255** (masked 148–164k of 640,000 px each). Per-intersection point + 4 footprint-interior offsets equal the effective over colour within 3/255 in both rasters: 3050 of 4250 samples checked in all three engines (1200 skipped — within 1.5 px of the sample's own footprint edge, the same sanity margin `crossing-proof` requires; Fixture F's real bands don't guarantee crossing-proof's margin at this gate's coarser mandated 800 px resolution, ¼ of crossing-proof's 8 px/mm). Footprint-edge 1-px band (each edge's midpoint, in both rasters) holds only O–U blends ≤ 6/255: max residual chromium 2.2/255, firefox 2.5/255, webkit 2.5/255 (1300 of 1700 samples; 400 skipped — same-material O/U pairs (blend line degenerate), a sample near a *different* eligible intersection's footprint, or near O's/U's own open segment endpoint — SPEC F's short, butt-capped accent-motif bands cross the lattice close to their own far end and to their own accent-a×accent-b crossing, neither of which the near-joint guard (SPEC §5.2, interior joints only) protects against; confirmed no third occurrence covers those points, and both engines agree on the same colours there, matching the ≈0 whole-image diff). String checks: no `class=`, no `style=`, every `url(` is `url(#…)`, `width`/`height` in mm match the viewBox, ids share one prefix and are unique. Clicking **Export SVG** in the Project Menu downloads `Interlace.svg` starting with `<svg`. Full chromium suite re-run clean after: `pnpm test:e2e --project=chromium` → 66 passed / 10 skipped (touch-only tests, chromium-touch project). |
| G6 Authorability | pending Task 18 | |
| G7 Tablet | pending Task 19 | §15 touch assertions pass in chromium-touch. |
| G8 Persistence | PASS | Unit: `src/domain/validate.test.ts`, `migrate.test.ts`, `commands/property.test.ts` (validate(command(p)) over corpus incl. fixtures), `src/storage/local.test.ts`. Browser: `e2e/persistence.spec.ts` (11 tests: quota failure → unsaved status + Download; corrupt key → banner and recovered key byte-identical after an edit; failed Open leaves project and history untouched; New/Open confirmations and blank bypass; pagehide flush on reload; other-tab detection; filename sanitising) passed on chromium, firefox, webkit ×3. |
| G9 Performance | pending Task 20 | |
